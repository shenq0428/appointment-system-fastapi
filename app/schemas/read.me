第二步：为什么要写这个文件？（SA 的深度思考）
你可能会问：“为什么不直接在函数里写代码，非要建个文件呢？”

作为架构师，我们要考虑数据验证（Validation）：

安全防线：如果用户在“预约时间”里填了一串乱码，这个 Schema 会在进入业务逻辑前就直接拦截报错，保护后台。

自动文档：当你定义了 BaseModel，FastAPI 的 /docs 页面会自动显示这个接口需要哪些参数，甚至连日期格式都帮你标好了。

类型安全：在代码其他地方引用时，编辑器会提醒你：patient_name 是字符串，不是数字。
